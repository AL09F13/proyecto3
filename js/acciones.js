// JavaScript Document
$(document).ready(function(e) {
document.addEventListener("deviceready",function(){
	
	
	$('#tomorrow').on ('tap', function() {
	navigator.vibrate(400);

});  // tap btn_vibrar
	
	$('#worldd').on ('tap', function() {
	navigator.vibrate(400);

});  // tap btn_vibrar
	
	$('#ultra').on ('tap', function() {
	navigator.vibrate(400);

});  // tap btn_vibrar
	
	$('#parties').on ('tap', function() {
	navigator.vibrate(400);

});  // tap btn_vibrar
	
	$('#electric').on ('tap', function() {
	navigator.vibrate(400);
	
});  // tap btn_vibrar


   $('#inicio').on ('tap' , function() {
	  navigator.notification.beep(1);
	  

}); //tap btnbeep

 $('#inicio2').on ('tap' , function() {
	  navigator.notification.beep(1);
	  

}); //tap btnbeep

 $('#inicio3').on ('tap' , function() {
	  navigator.notification.beep(1);
	  

}); //tap btnbeep

 $('#inicio4').on ('tap' , function() {
	  navigator.notification.beep(1);
	  

}); //tap btnbeep

 $('#inicio5').on ('tap' , function() {
	  navigator.notification.beep(1);
	  

}); //tap btnbeep

	
	
}); 
});

